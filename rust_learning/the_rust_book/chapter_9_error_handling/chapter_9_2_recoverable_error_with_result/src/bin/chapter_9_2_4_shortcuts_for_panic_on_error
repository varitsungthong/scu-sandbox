use std::fs::File;
//unwrap and expect both call panic! macro on error
//but expect method can give more context about why the operation is expected to always succeed
fn main() {
     let greeting_file = File::open("hello.txt")
        .expect("hello.txt should be included in this project");
}