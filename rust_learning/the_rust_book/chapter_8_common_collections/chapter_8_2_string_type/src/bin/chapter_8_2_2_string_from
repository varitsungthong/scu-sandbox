fn main() {
    let s = "initial contents".to_string();
    println!("{}",s);
    //string::from and to_string do the same work
    let m = String::from("ภาษาไทย");
    println!("{}",m)
    //string are utf-8 encoded so any proper encode data can be include
}