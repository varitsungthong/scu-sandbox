#[derive(Debug)]
struct Rectangle {
    width :u32,
    height :u32,
}

fn main() {
    let rect1 = Rectangle{
        width : 67,
        height : 69,
    };

    println!("The area of rectangle is {rect1:#?} square pixels.");
    dbg!(&rect1)
}