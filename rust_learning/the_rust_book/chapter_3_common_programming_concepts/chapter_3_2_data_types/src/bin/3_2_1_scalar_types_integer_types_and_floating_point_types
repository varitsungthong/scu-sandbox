fn main() {
    println!("Hello, world!");
    let guess:u8 = "42".parse().expect("Not a number!");
    println!("Guess equal to {guess}.")
    //i is signed integer
    //u is unsigned integer
    //number behind i and u specify how many bit the data type have
    //f is floating-point numbers, which are numbers with decimal points
    }