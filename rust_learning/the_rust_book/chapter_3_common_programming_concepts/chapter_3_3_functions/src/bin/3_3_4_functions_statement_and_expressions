fn main() {
    let y = {
        let x = 3;
        x + 1
    };

    println!("The value of y is: {y}");

//statement doesnt return value
//Expressions do not include ending semicolons if semicolons are added it will turn to statement
}